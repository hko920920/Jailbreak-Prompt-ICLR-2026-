# D3 Seed-23 Plan Bootstrap and Execution V2

## Status

**ADAPTIVE PLAN EXACTLY RECONSTRUCTED; 521-RECORD TARGET GENERATION IN PROGRESS**

This is an operational continuation of the frozen D3 exact-topology contract. It changes no
scientific population, adaptive rule, prompt, model, decoding parameter, evaluator, panel rule, or
gate.

## Pre-inference launcher stop

The first seed-23 bounded-launch attempt stopped before loading or invoking the target model because
the V1 operational launcher tried to load `phase_23_plan.safe.jsonl` before the frozen adaptive
planner had created that later-seed plan. Seed 11 had hidden this assumption because its full plan
was created during preflight. The failed attempt committed zero seed-23 model records and left no
model process.

V2 calls the existing frozen `run_d3_exact_topology.ensure_phase_plan` implementation first,
verifies that the persisted rows equal the returned rows, and then delegates unchanged execution to
the already tested V1 bounded launcher. The frozen scientific runner and contract remain
byte-identical.

Authoritative operational amendment:
`configs/natural_language_localization/d3_generation_plan_bootstrap_operational_v2.json`.

## Independent seed-23 denominator audit

An implementation-independent safe-side comparison formed the exact group key from instance ID,
ordered selected-unit tuple, and neutralizer ID. It filtered the 888 seed-11 plan rows only when the
corresponding seed-11 decision was panel HARMFUL, measurement-eligible, and target-nontruncated.

| Check | Result |
|---|---:|
| Seed-11 plan rows | 888 |
| Valid HARMFUL witness groups | 367 |
| Expected retained seed-23 groups | 521 |
| Persisted seed-23 plan rows | 521 |
| Missing groups | 0 |
| Unexpected groups | 0 |
| Order mismatches | 0 |
| Unique seed-23 record IDs | 521 |

Seed-23 plan SHA-256:
`969dfd98fbae1af55afcea9f86e2732af90e1c511005387b9956a448c6834de9`.

The retained denominator contains 447 DeepInception and 74 h4rm3l records. The first target chunk
was launched only after focused tests, Ruff, dependency-hash verification, the exact plan audit,
and a clean GPU-resource check passed.

The first bounded chunk then committed execution orders 0--63 and stopped cleanly at 64/521. The
audit found 64 unique records in exact plan-prefix order, 64/64 matching private-record hashes,
64/64 operational and evaluator-eligible rows, zero target truncations, zero temporary files, zero
residual experiment processes, and zero atomic-replace retries. Mean target inference time was
11.804 seconds per record. The verified checkpoint is
`data/natural_language_localization/d3_exact_topology_v1/phase_23_target_chunk_000000_000063.safe.json`;
the next execution order is 64.

The second bounded chunk committed execution orders 64--127 and stopped cleanly at 128/521. The
same cumulative audit passed over all 128 rows with zero plan-prefix, private-hash, operational,
eligibility, truncation, temporary-file, residual-process, or storage-retry defects. Mean target
inference time for the new 64 rows was 10.966 seconds. The next execution order is 128.

The third bounded chunk committed execution orders 128--191 and stopped cleanly at 192/521. The
same cumulative audit passed over all 192 rows without a defect; new-record mean target inference
time was 10.615 seconds. The next execution order is 192.

The fourth bounded chunk committed execution orders 192--255 and stopped cleanly at 256/521. The
same cumulative audit passed over all 256 rows without a defect; new-record mean target inference
time was 10.910 seconds. The next execution order is 256.

The fifth bounded chunk committed execution orders 256--319 and stopped cleanly at 320/521. The
same cumulative audit passed over all 320 rows without a defect; new-record mean target inference
time was 9.407 seconds. The next execution order is 320.

The sixth bounded chunk committed execution orders 320--383 and stopped cleanly at 384/521. Two
transient Windows destination locks were recovered by the operational retry layer. The complete
cumulative audit then found zero plan, private-hash, operational, eligibility, truncation,
temporary-file, or residual-process defects. New-record mean target inference time was 10.111
seconds. The next execution order is 384.

The seventh bounded chunk committed execution orders 384--447 and stopped cleanly at 448/521. The
same cumulative audit passed over all 448 rows without a defect; new-record mean target inference
time was 9.108 seconds. The next execution order is 448.

The eighth bounded chunk committed execution orders 448--511 and stopped cleanly at 512/521. The
same cumulative audit passed over all 512 rows without a defect; new-record mean target inference
time was 6.376 seconds. The next execution order is 512, with nine target calls remaining.

The final target chunk committed execution orders 512--520 and finalized the 521-record generation
artifact. The final audit found 521 unique records in exact plan order, 521/521 matching private
record hashes, 521/521 operational and evaluator-eligible rows, zero truncations, zero progress or
temporary residue, and matching data/summary identities. Summed per-record target inference time
was 1.426 hours. Generation SHA-256:
`4fe74be714dba9a0017b8355ae779c21039ddfc0dc32274e0104ece318807839`.

The next operation is the unchanged seed-23 Qwen3Guard axis; panel and topology outcomes remain
sealed.

The Qwen3Guard axis then completed 521/521 rows in 365.232 seconds. The independent integrity audit
found exact plan ID/order agreement, 521 unique rows, 521/521 strict safety and refusal parses, zero
output-limit stops, matching data/summary/contract hashes, no progress or temporary residue, and no
residual evaluator process. One transient destination lock was recovered by the operational
launcher. Data SHA-256:
`c63a310d34bf31a0f8ae8c3defa54abf6c172adb026cc3bbe0ac7ceeb140a665`.
Panel labels and topology meaning remained sealed. The next operation is the unchanged sequential
JailMeter axis.

The sequential JailMeter axis completed 521/521 rows in 5,097.751 seconds. The independent audit
found exact plan ID/order agreement, 521 unique rows, 521/521 strict binary parses, zero
output-limit stops, matching data/summary/contract hashes, no progress or temporary residue, and no
residual runner/server process. GPU memory returned to 414 MiB and Windows released the temporary
pagefile expansion after server shutdown. Data SHA-256:
`15bb21a0ac63abd1ad649efcb99b3dd61f279982bf4143b55523bdaaeb77f6e1`.

Both seed-23 evaluator axes are now immutable. The hash-guarded V1.1 phase finalizer then produced
HARMFUL 123, SAFE 337, and ABSTAIN 61; all 521 rows were measurement-eligible. An independent
implementation reconstructed every panel field from the generation and two evaluator-axis rows
without calling the finalizer's panel function. It found zero record-ID/order, metadata, response
hash, or panel-decision mismatches, and all result/hash bindings matched.

The same independent audit reconstructed the adaptive route from the original 888 seed-11 groups.
Seed 23 had zero missing, unexpected, or reordered groups. The 123 new valid nontruncated HARMFUL
witnesses do not overlap the 367 seed-11 witnesses, so exactly 398 groups continue to seed 47: 333
DeepInception and 65 h4rm3l; 192 use `LAYOUT_PRESERVING_BLANK` and 206 use
`SOURCE_AWARE_OMIT`. No observation was synthesized for a skipped seed. The immutable checkpoint is
`data/natural_language_localization/d3_exact_topology_v1/phase_23_finalization_checkpoint.safe.json`.
The next operation is the exact seed-47 plan and bounded target generation.

## Interpretation boundary

The 521 rows are repeated-seed robustness measurements of exact groups that have not yet received a
valid HARMFUL witness. They are not 521 independent prompt instances. Seed-23 generation alone opens
no topology label. The unchanged Qwen3Guard and JailMeter axes and the conservative phase finalizer
must complete before any further adaptive pruning or recurrence statement.
