# Prospective technical continuation after the seed11 resource stop

Prepared during the authorized eight-hour work interval, before any resumed
request or combined phase outcome. This document alone authorizes no execution;
the separate execution amendment must pin final code, this protocol, the parent
contract, exact existing SAFE prefix and the unchanged phase11 plan.

## What failed and what is retained

Parent scientific contract:
49bfa0302681971ed49150a95d3a300d3f8f4bc85e0fe483ec73ebd39e1b7139.
Seed11 target90 and Qwen85 calls completed; five target-length cases were skipped.
JailMeter completed30 requests, plus those five skips, before its resource
sampler stopped on GPU_RESOURCE_GATE at17:07:11UTC. The416 accepted samples
reached84C and5010MiB. The rejected sample was not retained: the precise failed
component and its time are unknown. An actual thermal crossing is plausible,
not proven. Matched PID58192 lifecycle receipts establish owned-process cleanup.

The old axis remains an OPERATIONAL FAILURE. All69 existing SAFE prefix files,
all existing request/reply files, abort, resources, server log and parent contract
remain immutable. The old worker/verifier must continue to reject that failed
axis. No failed resource criterion is reclassified as passed.

## Scientific invariants and scope

All45 exposed P, both conditions, seeds11/23/47, exact target/evaluator weights,
templates, numerical decoding settings, two-axis decision rule, truncation and
abstention handling, six-pair floor, full history and all-advanceable selection
are unchanged. No judge or target cycling, threshold relaxation, favorable
subset, replacement response or relabeling is allowed. The global270-call ceiling
per scientific target/axis and22:55:47UTC new-dispatch deadline remain unchanged.
No sealed/core/historical raw reopening, remote call, download or human label.

All30 completed original JailMeter responses are reused, regardless of their
labels, only after exact NEW request/raw-response/parser reconstruction. All5
existing skips are preserved. Exactly the55 previously undispatched seed11 IDs
form the continuation frame. No dispatched, incomplete or ambiguous request is
replayed. Later seeds, if the unchanged pair-state rule permits them, use this
same cooling policy prospectively. A new resource/transport/identity failure
closes the affected continuation epoch and requires review; there is at most
one continuation process epoch per seed, with no automatic relaunch.

Reusing returned responses does not establish the original resource gate passed.
Resource protection is distinguished from answer parsing and scientific labels:
the original epoch returned complete, hash-bound responses before it stopped,
but its failing sample cannot be localized precisely. All such responses remain
explicitly marked as originating in that failed epoch. A separate conservative
diagnostic treats all30 original judge decisions as UNKNOWN at seed11, and the
final stable-pool diagnostic reports how many certified pairs depend on those
responses. This diagnostic never replaces primary observations or selects which
responses to keep. No confirmation or hardware-reliability claim follows.

## New operational policy

Before EVERY new JailMeter request, wait until an observed temperature is at
most60C, with0.5-second polling and a maximum180-second cooling wait. Keep the
original sampled temperature<85C, GPU-used<=7600MiB, total>=8000MiB, baseline
GPU<=1000MiB, disk floor, and all other fixed resource limits. Record sampled
values/times and the actual rejected sample if a new gate fails. Sampling is
not continuous and cannot certify an unsampled maximum. No driver, registry,
fan, power, clock or thermal-protection setting is changed. The model remains
resident during ordinary per-request cooling; initial server launch still
requires the original idle/baseline gate. Fixed240-second in-flight timeout is
not shortened by the work-window deadline.

## Append-only execution authority and evidence

A new code-pinned continuation layer calls unchanged prompt/request/parser
helpers; it never monkey-patches or edits the frozen worker. Existing scientific
request IDs and per-request schemas remain unchanged. Each new request receives
a separate write-once execution-ownership sidecar binding the amendment, seed,
request ID and new owned process epoch. New start/stop/log/resource/abort files
live in a distinct continuation namespace. Composite verification validates the
old prefix against its original stopped lifecycle and every new request against
its new lifecycle and authority, then reparses all raw receipts.

The composite axis has a distinct schema/file, not the missing original axis
success file. The amendment explicitly permits FIRST writes to the previously
unwritten phase measurement/result/proof paths in the existing scientific
namespace. These results and proofs include the amendment SHA list, a false
original-seed11-operational-pass flag, and explicit verification scope:
RAW_SCIENTIFIC_RECEIPTS_AND_AMENDED_EXECUTION_NOT_ORIGINAL_OPERATIONAL_PASS.
Their identities include that disclosure. This is not a claim that the frozen
v1 runner produced or verified them. Existing parent artifacts are not replaced.
The unchanged phase-state algorithm may consume these disclosed results; the
new finalizer independently verifies current decisions, all45 histories,
predecessor identity and amendment ancestry, and writes the proof LAST.

Future topology preparation must propagate this execution deviation; it must
not turn the result into a clean original-contract pass. All resulting evidence
is already-exposed, result-informed DEVELOPMENT, not independent confirmation.

## Prerequisites

Before new dispatch: focused synthetic tests, independent review, exact69-file
prefix and90-row phase-plan pins, no ambiguous existing dispatch, unchanged code
closure and idle-resource check, prospective frozen amendment and recorded
authority. Any new scientific rule or materially different operation requires
a separate decision, not this technical continuation.
