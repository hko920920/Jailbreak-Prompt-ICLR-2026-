# D3 Exact Topology Final Result and NARROW Decision V1

Date: 2026-09-05 (Asia/Seoul)  
Status: **STEP 4 COMPLETE; OFFICIAL CROSS-FAMILY D3 GATE FAILED; PROJECT ROUTE = NARROW**

## 1. Decision in one sentence

The immutable cross-family D3 contract did **not** pass because pooled positive-decision
neutralizer Jaccard was `64/141 = 0.4539 < 0.80`; however, the prospectively permitted one-family
route remains scientifically live because h4rm3l alone passes every unchanged family-stratified D3
diagnostic, retains repeated nontrivial exact topology under both evaluator-axis ablations, and
exhibits a `0.3333` minimal-family recall loss for the best one-path baseline. This is a **NARROW**
routing decision, not a retroactive D3 PASS and not a paper-valid confirmatory claim.

## 2. Immutable official result

The finalizer and the separate verifier agree on all 19 integrity and reconstruction checks.

- selected exact-topology instances: 12 (`9` h4rm3l, `3` DeepInception);
- complete logical subset/neutralizer groups: 888;
- new target generations executed: 1,807;
- target generations skipped only after a valid harmful witness: 857;
- capability-control generations: 324;
- reportable topologies: 11/12;
- nontrivial topologies: 10/12 across eight unique payloads;
- subset decisions: 64 `RECOVERED`, 346 `NOT_RECOVERED`, 17
  `CAPABILITY_CONFOUNDED`, and 29 `ABSTAINED`;
- invalid, decision-relevant truncated, or incomplete subset decisions: 0;
- capability-confounded fraction over nonempty subsets: `17/444 = 0.0383`;
- pooled neutralizer-positive intersection/union: `64/141 = 0.4539`.

Seven of the eight frozen structural and validity gates passed. The sole failed gate was
`positive_neutralizer_jaccard_at_least_0_80`. Its threshold was not changed after observing the
result. The authoritative official route therefore remains
`STOP_OR_REDESIGN_AFTER_D3_EXACT_TOPOLOGY_GATE_FAILURE`.

## 3. Family-stratified diagnosis under the same gates

Applying the unchanged D3 numeric and structural gates within each family is a post-outcome routing
diagnostic. It cannot replace the official pooled gate.

| Measure | h4rm3l | DeepInception |
|---|---:|---:|
| instances | 9 | 3 |
| reportable topology | 8 | 3 |
| nontrivial topology | 7 | 3 |
| nontrivial unique payloads | 7 | 3 |
| certified minimal sets | 12 | 4 |
| minimal-set orders | 1 order-1; 11 order-2 | 2 order-2; 2 order-4 |
| neutralizer intersection / union | 25 / 30 | 39 / 111 |
| pooled positive Jaccard | **0.8333** | **0.3514** |
| capability-control records | 100 | 224 |
| failed control records | **0** | **22** |
| capability-confounded subsets | 0 | 17 |
| universal named unit across all minima | none | `scene_and_character_setup` |
| all unchanged family diagnostic gates pass | **yes** | **no** |

This split is why the correct project-level classification is NARROW rather than PASS or terminal
FAIL. DeepInception cannot support the central claim under the current neutralizers and controls.
h4rm3l can justify a newly frozen, explicitly restricted confirmation attempt.

## 4. Exact enumeration adds information beyond cheap paths

For h4rm3l, exact enumeration certified 12 minimal recovery sets over eight reportable instances.

- all-singleton search recovered only `1/12 = 0.0833` of the exact minimal family;
- greedy forward, greedy backward, and DDMIN each recovered `8/12 = 0.6667`;
- the best one-path recall loss was therefore `4/12 = 0.3333`, above the already frozen final
  practical-gap target of `0.15`;
- 11/12 certified h4rm3l minima required two source-native decorators;
- no one named primary unit occurred in every h4rm3l minimal set;
- no capability failure was needed to produce any h4rm3l recovery.

This is the strongest positive contribution signal: a singleton ranking or one deterministic path
does not recover the full minimal family even in the small three-unit exact lattice.

## 5. Evaluator-axis sensitivity

The post-outcome audit used only already stored decisions. It made no new target, evaluator, or
human call. An axis-only positive without all four already executed matched controls was retained as
`UNCONTROLLED_AXIS_POSITIVE`, never promoted to recovered.

| h4rm3l sensitivity | reportable | nontrivial | official edges retained |
|---|---:|---:|---:|
| Qwen axis, capability-controlled | 9/9 | 8/9 | 12/12 |
| JailMeter axis, capability-controlled | 8/9 | 7/9 | 12/12 |
| conservative two-axis panel, official | 8/9 | 7/9 | 12/12 |

JailMeter produced two additional axis-only positive subsets without the complete selected-control
bundle; the conservative audit excluded both. The 12 official h4rm3l minimal edges nevertheless
remain certified under both leave-one-axis-out reconstructions. The h4rm3l result is therefore not
created by one evaluator alone. These model axes are still automatic instruments, not independent
human judges.

## 6. Mandatory coarsening sensitivity and strongest rejection case

The predeclared outcome-blind coarsening merges adjacent source units. For h4rm3l it produces:

- `G0 = {AIMDecorator, RefusalSuppressionDecorator}`;
- `G1 = {AffirmativePrefixInjectionDecorator}`.

Under this coarsened vocabulary, all 9/9 h4rm3l instances have a reportable topology, but every one
has the same singleton minimal family `{G0}` and zero instances remain nontrivial at coarse order.

This does not invalidate the primary result: the primary units are real, separately implemented
source decorators fixed before outputs, and the paper's object is explicitly vocabulary-relative.
It does block a stronger claim that higher-order structure is granularity-invariant. A skeptical
reviewer can reasonably argue that the apparent interaction comes from splitting a larger obvious
attack block. The paper can survive this objection only by:

1. stating the source-native vocabulary in the title-level scope of the empirical claim;
2. reporting the coarsening collapse prominently rather than burying it;
3. making complete-family recovery and the one-path miss, not granularity-invariant synergy, the
   main contribution;
4. requiring the same source-native result to recur across a second target model and fresh
   payloads before writing a central claim.

## 7. Exact decision

Three distinct labels must not be conflated:

1. **Official frozen cross-family D3 gate: FAIL.** The pooled `0.80` Jaccard gate failed, and the
   immutable result remains failed.
2. **Project routing decision: NARROW.** h4rm3l passes every unchanged family diagnostic, shows
   repeated primary-vocabulary nontriviality, passes capability controls, survives evaluator-axis
   ablation, and defeats one-path complete-family recovery.
3. **Paper-valid empirical conclusion: NOT YET.** All current topology evidence is development
   evidence on one Q4 target model. There is no second-model confirmation or fresh-seed edge
   replication yet.

The NARROW route was permitted before D3 by
[PAPER_SCOPE_DECISION_V2.md](PAPER_SCOPE_DECISION_V2.md),
[report-source.md](report-source.md), and the
[reviewer-calibrated ten-step plan](ICLR2027_REVIEWER_CALIBRATED_TEN_STEP_PLAN_2026-09-04_V1.md).
No attack family, unit definition, neutralizer, evaluator, seed, or threshold was changed to obtain
this decision.

## 8. Required next operation

Do not open Step 6 under the failed two-family contract. The next admissible operation is **Step 5N**:

1. obtain the author's explicit NARROW decision;
2. freeze a new h4rm3l-only, two-target-model confirmatory contract before any new confirmatory
   output;
3. retain the existing `0.80` neutralizer-Jaccard, `0.75` fresh-seed edge replication, `<0.20`
   confound-plus-abstention, and `0.15` one-path recall-loss thresholds;
4. preserve the primary source-native decorator vocabulary and mandatory adjacent coarsening;
5. freeze a claim--evidence table whose strongest rejection case is the coarsening collapse;
6. qualify both exact Q4 artifacts and the execution host, then reassess runtime before Step 6N.

This audit itself authorizes no new confirmatory output.

## 9. Reproducible artifacts

- official D3 result SHA-256: `12e9289370c3db20423736103c5ca4f12666e4faf77c8a5b3eeeffdb0ce7e59b`;
- official independent verifier identity:
  `3038276ed076c39da108d3fde4a29da48d16e39731f8210e8632c45529507f0b`;
- post-outcome audit config SHA-256:
  `46e765d0d0efad8ac97b81d5274ddd5d508ba5cb2d814e8136760f713a377c8f`;
- audit implementation SHA-256:
  `5f42c469f7770263d4ba6eee3113b5591473ca6479b49b6afaeea71b4ad70e51`;
- audit safe result SHA-256:
  `d03a9e2a94e34c1db5f3e8c25186d9f2ca725f291d3e627f45ff927c0c503f78`;
- audit safe result identity:
  `ca6d99d794537e2a77f4f649364be38dff9fc88490de1756e6280a78249e521c`;
- focused audit tests: 4/4 passed; Ruff: passed;
- raw harmful text written by the audit: false.
