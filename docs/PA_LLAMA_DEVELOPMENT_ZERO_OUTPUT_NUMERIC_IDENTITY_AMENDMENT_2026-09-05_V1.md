# All45 development: zero-output numeric-identity amendment

Recorded 2026-09-05 16:40 UTC, before any input extraction, metadata census,
scientific response or evaluator response in this experiment.

## Preserved first attempt

The first frozen execution contract was33,171bytes,SHA-256
0728d814a5fc050ffa67d354e570827afa2463710fe36a9248bfea767b7d448c,
frozen2026-09-05T16:36:59+00:00. Its exact bytes are preserved in
[the first-attempt archive](../configs/natural_language_localization/pa_llama_development_screen_v1_preflight_attempt_01.json).
Root verified the archive's full-file SHA equals that first frozen SHA.

Both target and panel static preflight commands stopped with
FIXED_PANEL_RUNTIME_CHANGED. Their shared contract loader compares canonical
JSON bytes. The qualified JailMeter runtime has temperature0.0; serialization
through the JavaScript orchestration layer produced temperature0. Those values
are numerically equal, but their Python canonical JSON byte representations
are distinct. Recursive comparison found no other target/panel runtime-field
difference. The stricter original identity check is retained.

Both commands failed inside load_contract before returning to any extraction,
census, model loader, process launcher or dispatch routine. No experiment
reservation, new experiment safe/private directory or inference receipt was
created. Independent metadata checks confirmed those paths were absent. These
checks plus the executed code path support0newscientifictarget,0judge,
0historical-private reads and0sealed reads for these failed preflights; filesystem
absence alone is not offered as proof that arbitrary historical reads never
occurred. The separate already completed six harmless smoke calls are unchanged.

## Bounded amendment

Under the author's eight-hour instruction to repair technical errors, replace
only the JailMeter temperature literal0 with the qualified source literal0.0.
Also update the execution contract's freeze timestamp and pin this amendment
document as provenance. No scientific field's numerical value, model, panel,
prompt, tokenizer, parser, criterion, source population, seed, call ceiling,
resource floor, deadline or executable code changes. No compatibility rule is
weakened. All old scientific/admission failures and the failed first preflight
remain recorded.

The canonical execution filename remains the supported runner entrypoint; its
new SHA must be supplied explicitly. The archived first attempt cannot execute
through that entrypoint because its SHA no longer matches the canonical file.
There was no dispatch reservation to reset and no stochastic response to repeat.

After independent exact-difference review, rerun zero-generation preflight on
the amended SHA. Proceed to restricted90-input extraction and all90census only
if it passes. Record amended file identity and actual subsequent counts in
[the append-only live record](PA_EIGHT_HOUR_RESEARCH_LIVE_RECORD_2026-09-05_V1.md).
