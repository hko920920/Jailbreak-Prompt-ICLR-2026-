# P/A topology target continuation: prospective technical amendment

This document describes a separate technical repair after an observed resource
failure. It is not execution authority on its own. The exact new code, tests,
prefix manifest and this protocol must be pinned in a separately frozen contract
after review. The original execution protocol, source files, receipts and failure
are immutable. No model call is permitted by an unfrozen template.

## Authority, question and unchanged deadline

The author's eight-hour instruction includes correcting technical errors and
continuing the original fixed-P/attack-added-A-unit research. It does not include
fabricated positive evidence, destructive cleanup, paid calls, sealed data,
manuscript/PDF work or extending the dispatch deadline beyond
2026-09-05 22:55:47 UTC. That boundary remains binding on all new target and
evaluator dispatches. In-flight requests retain their original timeout and owned
cleanup; they are not artificially truncated at the deadline.

Retain all 21 admitted, already-exposed development P cases, all masks, both
operators, all seeds, both controls, the original deterministic request order,
and the original two-evaluator rules. P bytes and attack-unit ownership do not
change. The D3-compatible primary rule, control-qualified secondary tables,
minimality and same-six-witness C3 criteria remain unchanged. This remains
exposed DEVELOPMENT after prior target/measurement failures, not confirmation
or an ICLR paper-validity gate.

## Actual failure and immutable prefix

Original topology contract:
d05f91c14625217e191837c0abbef0bc2e78eebaa7cecec18c97ce30b0cf19f0.
Bound-plan file SHA:
3a8bc4391f2ba6d02f2de446bd8b4ff61d1c1ac121fb3c57044bee379df1615b.
Bound-plan identity:
8b3b702ab5ea256699b9c3a92b148f05feb0523bd903724dd7ae91dfa9c789f9.

The original target run returned 872/1470 responses: 536 science, 336 controls.
Ordinal 873 failed before dispatch in its cooling/resource check. Its ID is
7a56b81d4e57e5d6526493e23996f5ad3f8567fc83b24e5435285951d75ecf60.
The rejected sample at 20:26:46.865429 UTC records 16094355456 free disk bytes,
below the unchanged 16106127360-byte floor, with GPU 5416 MiB and 60 C.
Owned PID 32748 stopped at 20:26:47.711428 UTC. Original generate-aborted
records DISK_FREE_SPACE_BELOW_15_GIB and scientific_gate_evaluated:false.

Post-cleanup free space recovered automatically to 20.965 GiB and pagefile
allocation fell to 45847 MiB. No file deletion, registry/pagefile/hardware change
or unrelated process termination occurred. These observations support a resource
repair attempt, not a proved diagnosis or guaranteed prevention of recurrence.

The read-only SAFE inventory found a stable completed prefix 1-872, only a
predispatch remainder at 873, no SAFE dispatch for 874-1470, no ambiguous SAFE
dispatch-completion states, no active lock and no unclosed worker marker.
Its identity b61f428a6283899e4edb459d433cc8922983ab8926b763967e726cbc10f2401d
is progress accounting only, NOT raw receipt certification. It does not prove
absence of unjournaled activity by itself.

Before continuation, a separate exact manifest must pin all relevant original
NEW-experiment private request/reply bytes and SAFE dispatch/row/template/
cooldown receipts, census/materialization/source bindings, epoch lifecycle,
abort and failed-873 cooling evidence. Verify namespace completeness, absence
of request/dispatch/reply for 873 and later, and the frozen code's journal-before-
call ordering. Any unexplained file, ambiguous dispatch, changed hash, incomplete
old response or uncertain owned cleanup blocks execution. No slot is retried
on the assumption that it probably did not complete.

Reparse all 872 original raw responses with the pinned rules and recheck their
requests, native census, model/template identities, timestamps, accepted resource
samples and owned lifecycle. Reuse all first responses regardless of content,
length, refusal, control result or future evaluator label. Reference the original
files; do not create raw-response copies or rewrite any original receipt. Preserve
the original failed cooldown even though it cannot certify a new dispatch.

Historical 90-file extraction, old D3/C1N response readers, GuidedBench core,
Primary A60 and Reserve B60 remain forbidden. Only the explicitly manifested
NEW screen/topology artifacts may be read by approved new verifiers. No original
full-suite, old private reader, model download, paid remote call or human label.

## Exact suffix and prospective bounded process epochs

Dispatch exactly ordinals 873-1470, once each, in original order: 598 targets,
comprising 346 science and 252 controls. The overall target ceiling remains
1470 = 872 retained + 598 new; Qwen and JailMeter retain their complete 882-row
science frames. Original native census is reverified, not regenerated. No new
answer-bearing context probe, deduplication, best-case subset or early harmful-
witness stopping is introduced.

Use fixed suffix process epochs, independent of all response outcomes:

| New outer epoch | Original ordinals | Target calls |
| --- | --- | ---: |
| 003 | 873-1082 | 210 |
| 004 | 1083-1292 | 210 |
| 005 | 1293-1470 | 178 |

Every epoch has its own exclusive paths, pinned command, owned PID, start/stop
and identity/resource evidence. Stop and verify only the owned process, then
check the next prelaunch baseline before the next fixed chunk. All original
calls use their original request IDs, seeds and scientific configuration, plus
an explicit separate amendment binding. No original failed file is overwritten.
The amendment binding belongs in authority/provenance receipts, not in altered
scientific prompt bytes or decoding request fields.

Before each new target epoch require at least 20 GiB free C space, a stricter
admission check than the original 15 GiB per-request floor. Retain the original
prelaunch GPU <=1000 MiB, sampled GPU <=7600 MiB and temperature <85 C, CUDA
bound where applicable, and temperature <=60 C before EVERY target/evaluator
dispatch (0.5-second polling, maximum 180 seconds). Preserve rejected samples.
The 20 GiB check and <=210-call chunks aim to bound resource accumulation; they
do not prove the underlying cause or guarantee successful completion.

No fan, power, driver, registry, pagefile or unrelated process/file modification.
A new resource, identity, transport or deadline failure leaves a new immutable
abort and forbids automatic reentry/replay. Missing or failed writes are evidence
gaps, not permission to fabricate replacement receipts. Deadline applies inside
each chunk and before each subsequent launch/dispatch.

## Explicit composite verification, never original-operation PASS

Keep original target operational_gate_passed:false. The unmodified original
worker, panel entry and finalizer must continue to reject the original failed
operation. Do not hide its abort/orphan through an adapter that returns generic
operational_gate_passed:true under the old meaning.

A new composite verifier must explicitly distinguish:

- All 872 retained NEW raw target receipts (536 science, 336 controls) reverified, with the original
  generation operational failure still FALSE for pass.
- All 598 suffix receipts and their separately admitted owned execution,
  verified against the fixed suffix and amendment contract.
- Exact full 1470-row completeness, order, uniqueness, budget, source identity
  and no ambiguous/missing calls under the declared amended execution.

The new gate is specifically an amended full-target receipt/execution gate.
It must expose the amendment SHA, original failure and failed-cooling hashes,
prefix/suffix identities, counts, epoch mapping and precise verification scope.
No incomplete prefix can receive that full gate or a scientific success label.

Any downstream evaluator needs a separately reviewed entry point accepting
that explicit amended gate. It must not call the original failed entry as if
the old contract passed. Both complete target receipt chains are prerequisites
to either judge. Qwen then JailMeter retain exact original full-P/full-response
inputs, model/runtime/prompt/parser, caps, context rules, deterministically
justified skips and full 882-row accounting. Controls are never judged for
harmfulness. An unrun judge is not an ABSTAIN observation.
An explicit frozen-rule ineligible-target skip remains distinct from an
unfinished eligible evaluation. Such a skip can produce a downstream ABSTAIN
under the existing rule, but must never be reported as an observed judge reply.

Use new amendment paths for axes and final products. Every new axis/public
scientific product/proof must carry the target amendment and original failure,
in addition to the separate preexisting screen JailMeter amendment. Reuse the
pinned pure measurement/analysis rules without rescoring historical failures.
Recompute all dependent identities after disclosure is incorporated, verify
cross-file joins and exact canonical bytes, and publish proof LAST. Do not issue
the old unqualified success proof or claim compatibility with an unmodified
report inspector that does not understand the amendment schema.

## Interpretation, timing and stop branches

The suffix boundary is chosen solely by the observed resource failure, not by
content or label quality. Nonetheless restart boundaries are not randomized.
Masks 1-4 are entirely in the original epoch, mask 5 straddles original/new
execution, and later masks straddle the fixed suffix chunks. Runtime state,
cache/RNG implementation and epoch may interact with position or condition.
Do not assert equivalence to an uninterrupted run, epoch-invariant effects,
causal security mechanisms, or six independent epoch replications. Request-level
seeds and identity checks do not by themselves prove those stronger properties.

The initially considered mask-boundary chunks were revised BEFORE freeze or
any suffix call to fixed <=210-call chunks. The revision avoids assigning all
of mask 6 or 7 to a unique new epoch, but does not eliminate epoch confounding.
No scientific request order, criterion or case membership was changed.
Specifically, the first 32 requests of a mask are part of its seed-11 science
slot (one whole operator/P block and 11 P from the other operator); the later
178 include the remaining seed-11 cells, both controls and seeds 23/47. New
epoch 003 contains the last 178 of mask 5 and first 32 of mask 6, epoch 004 the
last 178 of mask 6 and first 32 of mask 7, and epoch 005 the last 178 of mask 7.
This is resource-bounded segmentation, not balanced or randomized epoch
replication; seed/operator/P portions remain aligned with epoch boundaries.

Remaining time may suffice only for code verification and target collection.
There is no expectation or guarantee of all judge calls and C3 finalization
before 22:55:47 UTC. If the deadline or another gate prevents completion,
retain the full original plan and explicit incomplete/unknown accounting;
do not reduce sample size, alter scoring or call a target-only run C3 evidence.
Report exact completed calls, remaining stages and the new blocker. Time spent
and working tests are not evidence that the paper contribution is established.
